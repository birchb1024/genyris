defvar 'the (lambda (x) x)

defvar '$global 999

defvar 'Standard-Class (dict)

defvar 'Account
    dict
        .classes (cons Standard-Class nil)
        .print
            lambda () (cons $global .balance)
         
Account
    defvar '.new
        lambda (initial-balance)
          dict
            .classes (cons Account nil)
            .balance initial-balance
            
defvar 'bb 
  Account (.new 1000) 
  
bb(.print)

bb((lambda () .balance))

bb (set '.balance 34)

bb.balance






