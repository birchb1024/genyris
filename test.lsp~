((lambda ()

(define 'null (x) (cond (x nil) (t t)))

(define 'last (x)
    (cond
        ((null (cdr x)) (car x))
	(t (last (cdr x)))))

(cond 
	((last '(1 2 3)) "Test Passed") 
	(t "Test Failed"))

))




