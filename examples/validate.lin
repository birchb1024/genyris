;
; How the _valid? method is called by the tagging operator and tag
;

class C()
    def _valid? (self)
        > self 23

; call the validator directly from the class env
C (_valid? 34)   ; => true
C (_valid? 21)   ; => nil


; mark the number 24 as a member of class 'C'
tag 24 C      ; => ~ 24 ; Bignum C
24 : C        ; => ~ 24 ; Bignum C

22 : C      ; => *** Error: class validator error for object 22
