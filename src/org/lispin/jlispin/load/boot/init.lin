;
the "init.lin"

defmacro define (var valu) (backquote (defvar ',var ,valu))
    
defmacro if (test success-result failure-result)
    backquote
       cond
          ,test ,success-result
          t ,failure-result

     
